const mainArea=document.getElementById("mainArea"),lessonList=document.getElementById("lessonList"),resetBtn=document.getElementById("resetBtn"),homeBtn=document.getElementById("homeBtn"),reviewBtn=document.getElementById("reviewBtn");const STORAGE_KEY="hebrewCourseV2Progress",WRONG_WORDS_KEY="hebrewCourseV2WrongWords";function loadProgress(){return JSON.parse(localStorage.getItem(STORAGE_KEY)||"{}")}function saveProgressData(d){localStorage.setItem(STORAGE_KEY,JSON.stringify(d))}function loadWrongWords(){return JSON.parse(localStorage.getItem(WRONG_WORDS_KEY)||"[]")}function saveWrongWords(w){localStorage.setItem(WRONG_WORDS_KEY,JSON.stringify([...new Set(w)]))}function normalize(s){return(s||"").trim().replace(/[״׳"]/g,"").replace(/\s+/g," ")}function radioValue(n){const e=document.querySelector(`input[name="${n}"]:checked`);return e?Number(e.value):null}function renderLessonList(){const p=loadProgress();lessonList.innerHTML="";let u="";LESSONS.forEach(l=>{if(l.unit!==u){u=l.unit;const h=document.createElement("h3");h.textContent=u;lessonList.appendChild(h)}const b=document.createElement("button");b.className="lesson-btn"+(p[l.id]?" done":"");b.textContent=`${l.title}${p[l.id]?" ✓":""}`;b.onclick=()=>renderLesson(l);lessonList.appendChild(b)})}function renderHome(){const p=loadProgress(),done=Object.keys(p).length,s=Object.values(p),avg=s.length?Math.round(s.reduce((a,b)=>a+b.percent,0)/s.length):0,w=loadWrongWords();mainArea.innerHTML=`<div class="card"><h2>דף הבית</h2><p>קורס קיץ בעברית לדובר אנגלית. בכל שיעור יש אוצר מילים, קריאה, הבנת הנקרא, דקדוק ו-Cloze.</p><div class="grid"><div class="stat"><strong>${done}</strong>שיעורים הושלמו</div><div class="stat"><strong>${avg}%</strong>ממוצע</div><div class="stat"><strong>${LESSONS.length}</strong>שיעורים זמינים</div><div class="stat"><strong>${w.length}</strong>מילים לחזרה</div></div><h3>ציונים אחרונים</h3>${s.length?Object.entries(p).map(([id,item])=>{const l=LESSONS.find(x=>x.id===id);return`<p><strong>${l?.title||id}</strong>: ${item.score}/${item.total} (${item.percent}%)</p>`}).join(""):"<p>עדיין אין ציונים.</p>"}</div>`;renderLessonList()}function renderLesson(l){mainArea.innerHTML=`<div class="card"><h2>${l.title}</h2><p class="small">${l.unit}</p><div class="part"><h3>חלק א׳ – אוצר מילים</h3><p>התאם בין המילה בעברית לפירוש באנגלית.</p>${l.vocabulary.map((pair,i)=>`<div class="question"><strong>${i+1}. ${pair[0]}</strong><select id="vocab_${i}"><option value="">בחר פירוש</option>${l.vocabulary.map((p,j)=>`<option value="${j}">${p[1]}</option>`).join("")}</select></div>`).join("")}</div><div class="part"><h3>חלק ב׳ – קריאה</h3><div class="reading">${l.reading}</div></div><div class="part"><h3>חלק ג׳ – הבנת הנקרא</h3>${l.comprehension.map((q,i)=>Array.isArray(q)?`<div class="question"><strong>${i+1}. ${q[0]}</strong><div class="options">${q[1].map((o,j)=>`<label><input type="radio" name="comp_${i}" value="${j}"> ${o}</label>`).join("")}</div></div>`:`<div class="question"><strong>${i+1}. ${q.text}</strong><textarea id="open_${i}" rows="4" placeholder="כתוב תשובה קצרה"></textarea><p class="small">בדיקה בסיסית לפי מילות מפתח.</p></div>`).join("")}</div><div class="part"><h3>חלק ד׳ – דקדוק</h3>${l.grammar.map((q,i)=>`<div class="question"><strong>${i+1}. ${q[0]}</strong><div class="options">${q[1].map((o,j)=>`<label><input type="radio" name="gram_${i}" value="${j}"> ${o}</label>`).join("")}</div></div>`).join("")}</div><div class="part"><h3>חלק ה׳ – Cloze</h3><p><strong>בנק מילים:</strong> ${l.clozeBank.map(w=>`<span class="badge">${w}</span>`).join(" ")}</p>${l.cloze.map((q,i)=>`<div class="question"><strong>${i+1}. ${q[0]}</strong><input type="text" id="cloze_${i}"></div>`).join("")}</div><button onclick="gradeLesson('${l.id}')">בדוק שיעור</button><div id="resultBox"></div></div>`;scrollTo({top:0,behavior:"smooth"})}window.gradeLesson=function(id){const l=LESSONS.find(x=>x.id===id);let score=0,total=0,parts={vocab:[0,0],reading:[0,0],grammar:[0,0],cloze:[0,0]},fb=[],wrong=loadWrongWords();l.vocabulary.forEach((pair,i)=>{total++;parts.vocab[1]++;const v=document.getElementById(`vocab_${i}`).value;if(Number(v)===i){score++;parts.vocab[0]++}else{fb.push(`אוצר מילים: ${pair[0]} = ${pair[1]}`);wrong.push(pair[0]+" = "+pair[1])}});l.comprehension.forEach((q,i)=>{total+=2;parts.reading[1]+=2;if(Array.isArray(q)){const v=radioValue(`comp_${i}`);if(v===q[2]){score+=2;parts.reading[0]+=2}else fb.push(`הבנת הנקרא ${i+1}: התשובה הנכונה היא "${q[1][q[2]]}".`)}else{const ans=normalize(document.getElementById(`open_${i}`).value),hit=q.keywords.filter(k=>ans.includes(k)).length,pts=hit?2:(ans.length>10?1:0);score+=pts;parts.reading[0]+=pts;fb.push(`שאלה פתוחה ${i+1}: תשובה טובה תכלול רעיון כמו: ${q.keywords.join(" / ")}.`)}});l.grammar.forEach((q,i)=>{total++;parts.grammar[1]++;const v=radioValue(`gram_${i}`);if(v===q[2]){score++;parts.grammar[0]++}else fb.push(`דקדוק ${i+1}: התשובה הנכונה היא "${q[1][q[2]]}".`)});l.cloze.forEach((q,i)=>{total++;parts.cloze[1]++;const v=normalize(document.getElementById(`cloze_${i}`).value);if(v===q[1]){score++;parts.cloze[0]++}else fb.push(`Cloze ${i+1}: התשובה הנכונה היא "${q[1]}".`)});saveWrongWords(wrong);const percent=Math.round(score/total*100),p=loadProgress();p[l.id]={score,total,percent,parts,date:new Date().toLocaleDateString("he-IL")};saveProgressData(p);document.getElementById("resultBox").innerHTML=`<div class="scoreBox"><h3>ציון: ${score}/${total} (${percent}%)</h3><p>אוצר מילים: ${parts.vocab[0]}/${parts.vocab[1]} | הבנת הנקרא: ${parts.reading[0]}/${parts.reading[1]} | דקדוק: ${parts.grammar[0]}/${parts.grammar[1]} | Cloze: ${parts.cloze[0]}/${parts.cloze[1]}</p></div><h3>משוב</h3>${fb.length?`<ul>${fb.map(x=>`<li>${x}</li>`).join("")}</ul>`:"<p>מעולה! הכול נכון.</p>"}`;renderLessonList()};function renderReview(){const w=loadWrongWords();mainArea.innerHTML=`<div class="card"><h2>חזרה על מילים שטעיתי בהן</h2>${w.length?w.map(x=>`<span class="badge">${x}</span>`).join(" "):"<p>אין עדיין מילים לחזרה.</p>"}<p class="small">בגרסה הבאה אפשר להפוך את הרשימה הזאת לבוחן חזרה אוטומטי.</p></div>`}resetBtn.onclick=()=>{if(confirm("לאפס את כל ההתקדמות?")){localStorage.removeItem(STORAGE_KEY);localStorage.removeItem(WRONG_WORDS_KEY);renderHome()}};homeBtn.onclick=renderHome;reviewBtn.onclick=renderReview;renderHome();